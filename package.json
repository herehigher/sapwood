{
  "name": "sapwood-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "sapwood — the autonomous coding loop with governance built in",
  "workspaces": ["engine"],
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "typecheck": "npm run -ws typecheck",
    "test": "npm run -ws test",
    "build": "npm run -ws build"
  },
  "license": "MIT"
}
