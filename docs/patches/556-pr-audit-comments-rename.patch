# Pending human-applied patch — issue #556
#
# WHAT: renames the forge-proxy audit tool's wire name in the shipped
#       sapwood.config.yaml comment, from `getPRAuditComments` to
#       `pr_audit_comments`, matching the rename landed in engine/ and docs/ by
#       this issue's PR. The now-stale "[camelCase wire name, the odd one out;
#       #556 tracks normalizing it]" aside goes with it — there is no odd one
#       out any more (proxy/tools.test.ts now asserts snake_case over the whole
#       TOOL_NAMES set), and the surrounding comment lines reflow accordingly.
#
# WHY IT IS A PATCH AND NOT A COMMIT: sapwood.config.* is a guard-enforced
#       human-merge-only path (engine/src/guard/guard.ts protectedPathLabel),
#       as a WHOLE FILE — no per-field carve-out. An autonomous worker's write
#       is denied at the tool level:
#         BLOCK [write-path] sapwood.config.* (engine/guard config) is human-merge-only
#       The guard constrains Claude tool calls, never a human's editor. See
#       docs/security.md "sapwood.config.* is also the shipped starter config".
#
# APPLY (from the repo root), then delete this file:
#       git apply docs/patches/556-pr-audit-comments-rename.patch
#       git rm docs/patches/556-pr-audit-comments-rename.patch
#
# Generated against sapwood.config.yaml at blob 839a1a9. If the file has since
# moved, the hunk still locates by context (offset-tolerant); re-check the result.
diff --git a/sapwood.config.yaml b/sapwood.config.yaml
--- a/sapwood.config.yaml
+++ b/sapwood.config.yaml
@@ -523,9 +523,9 @@
 # set over a bearer-token-authenticated local HTTP server, minted per session, revoked at
 # teardown — issue-oriented tools (issue_details/issue_comments/issue_relations/search_issues,
 # #234) for peripheral roles, PR-facing tools (pr_details/pr_reviews/pr_review_threads/pr_checks/
-# getPRAuditComments [camelCase wire name, the odd one out; #556 tracks normalizing it],
-# #244 — the fix-loop worker leg's PR-review evidence channel) for the `worker` role. No gate/
-# verdict logic lives in any tool; that stays in reviewer.ts/merge-driver.ts.
+# pr_audit_comments, #244 — the fix-loop worker leg's PR-review evidence channel) for the
+# `worker` role. No gate/verdict logic lives in any tool; that stays in reviewer.ts/
+# merge-driver.ts.
 # proxy:
 #   enabled: true  # #551 default; set to false to opt out (fully inert)
 #   caps:
