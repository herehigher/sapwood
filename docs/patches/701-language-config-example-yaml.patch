# Pending human-applied patch — issue #701, PR TBD
#
# WHAT: adds the (commented-out, all-default) `language` config section to the shipped
#       `sapwood init` starter template, sapwood.config.example.yaml — the same
#       discoverability addition as the sibling 701-language-config-yaml.patch, applied to
#       the starter template every future `sapwood init` inherits.
#
# WHY IT IS A PATCH AND NOT A COMMIT: sapwood.config.example.* is guard-protected as a
#       sibling rule to the root config (engine/src/guard/guard.ts protectedPathLabel,
#       #781) — same whole-file, no-per-field-carve-out treatment. An autonomous worker's
#       write is denied at the tool level:
#         BLOCK [write-path] sapwood.config.example.* (init-starter template) is human-merge-only
#       The guard constrains Claude tool calls, never a human's editor. See
#       docs/security.md "The protected live config and shipped starter are separate".
#
# APPLY (from the repo root), then delete this file:
#       git apply docs/patches/701-language-config-example-yaml.patch
#       git rm docs/patches/701-language-config-example-yaml.patch
#
# Generated against sapwood.config.example.yaml at blob c814bba. If the file has since
# moved, the hunk still locates by context (offset-tolerant); re-check the result.
diff --git a/sapwood.config.example.yaml b/sapwood.config.example.yaml
index c814bba..ef4e6ad 100644
--- a/sapwood.config.example.yaml
+++ b/sapwood.config.example.yaml
@@ -44,6 +44,10 @@
   mode: produce-pr-and-stop

 # These are the documented defaults. Uncomment to override them.
+# language: # default: en for every surface — see docs/configuration.md's "language" section
+#   codeComments: en # BCP-47-ish tag (e.g. ja, zh-Hans, pt-BR); passed through opaquely
+#   issuesAndPrs: en
+#   docs: en
 # envFailure:
 #   llmPatterns: [rate_limit_error, "rate limit exceeded", "usage limit reached",
 #                 "credit balance is too low", insufficient_quota, overloaded_error,
