# Pending human-applied patch — issue #549
#
# WHAT: updates the commented `escalation.instructionPaths` example in the shipped
#       sapwood.config.yaml. The comment still says a repointed
#       `reviewer.agent.promptFile` is NOT covered and must be added by hand; #549
#       closed that gap (loadConfig now captures `reviewer.agent.promptFileRaw` and
#       effectiveInstructionPaths derives the pattern from it, exactly as it already
#       did for `doctrine.file`). The workaround must go with the gap, or the
#       shipped config keeps prescribing a hand-add that is no longer needed.
#
# WHY IT IS A PATCH AND NOT A COMMIT: sapwood.config.* is a guard-enforced
#       human-merge-only path (engine/src/guard/guard.ts protectedPathLabel).
#       An autonomous worker's write is denied at the tool level:
#         BLOCK [write-path] sapwood.config.* (engine/guard config) is human-merge-only
#       The guard constrains Claude tool calls, never a human's editor. See
#       docs/security.md "sapwood.config.* is also the shipped starter config".
#
# APPLY (from the repo root), then delete this file:
#       git apply docs/patches/549-prompt-file-raw-config.patch
#       git rm docs/patches/549-prompt-file-raw-config.patch
#
# Generated against sapwood.config.yaml at blob 839a1a9. If the file has since
# moved, the hunk still locates by context (offset-tolerant); re-check the result.
diff --git a/sapwood.config.yaml b/sapwood.config.yaml
index 839a1a9..0000000 100644
--- a/sapwood.config.yaml
+++ b/sapwood.config.yaml
@@ -575,11 +575,11 @@
 #   # recursive `**`. Paths must be trimmed canonical repo-relative forms (no ./, /, .. segment,
 #   # //, or trailing /). A list at GitHub's 3,000-file ceiling escalates as unverifiable. Set
 #   # to [] to deliberately disable the mechanism (then sapwood does not fetch changed files).
-#   # #527: the reviewer's OWN doctrine file is covered too, DERIVED from doctrine.file (so a
-#   # repointed doctrine path stays covered) — it is never listed here. engine/prompts/** covers
-#   # the shipped reviewer prompt; inert unless the target repo IS the engine's source tree. A
-#   # repointed reviewer.agent.promptFile outside that glob is NOT covered — add it by hand
-#   # (docs/security.md, "Which carriers are covered").
+#   # #527/#549: the reviewer's OWN carriers are covered too, DERIVED from doctrine.file and
+#   # reviewer.agent.promptFile (so a repointed path stays covered) — neither is listed here.
+#   # engine/prompts/** covers the shipped reviewer prompt; inert unless the target repo IS the
+#   # engine's source tree. A derived path outside the repo (absolute or ..-escaping) is skipped
+#   # rather than smuggled in (docs/security.md, "Which carriers are covered").
 #   instructionPaths: [CLAUDE.md, CLAUDE.local.md, .claude/CLAUDE.md, .claude/rules/**, AGENTS.md, engine/prompts/**]
 #   holdLabels: [sapwood:hold]  # #248: human-applied WAIT-tier hold (three-tier escalation
 #                                # model: hold/needs-human/blocked, each one fact). Matched by
