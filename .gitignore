node_modules/
dist/
dist-server/
# #742: dashboard/src/demo/export-cli.ts generates this from demo/source.ts at build time
dashboard/public/
*.tsbuildinfo
data/
*.sqlite
*.sqlite-*
.DS_Store
.codegraph/
.worktrees/

# python bytecode: guard.fuzz.test.ts's differential oracle is a static TS fixture since #840
# (guard.py is no longer vendored in-repo), but scripts/regen-guard-shared-block-fixture.ts still
# shells out to a human-supplied guard.py checkout when re-deriving that fixture.
__pycache__/

# Browser-automation session artifacts (claude-in-chrome / playwright-mcp)
.playwright-mcp/

# `npm run shots` (dashboard/shots) output — captures + generated contact sheet, regenerated
# on every run, never a committed artifact.
dashboard/shots-output/

# #1032: prepack copies the repo-root example config in for `npm pack`/`npm publish` so it
# ships inside the installed package — the repo root copy stays the single source of truth.
engine/sapwood.config.example.yaml
engine/dashboard-dist/
