# Single owner for the whole tree; split into per-path entries only when a second owner exists.
# Human-merge-only paths (docs/security.md) and escalation.instructionPaths are therefore covered by `*`.
* @herehigher
